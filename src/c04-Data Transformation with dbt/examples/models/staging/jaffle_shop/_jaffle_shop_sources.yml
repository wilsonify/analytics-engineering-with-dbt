# Source configuration for Jaffle Shop
# Note: Source tests are now omitted since data comes from dbt seeds
# See seeds/jaffle_shop/ for data definitions

version: 2

sources:
  - name: jaffle_shop
    tables:
      - name: customers
      - name: orders
